# Bugs

## Offen

(keine)

## Behoben

| Datum | Bug | Ursache | Lösung |
|---|---|---|---|
| 2026-09-17 | Änderungen (IP-Freigabe, Schutz aus, neuer localhost-Port) wurden direkt nach dem CLI-Aufruf noch nicht wirksam | `systemctl reload nginx` kehrt zurück, bevor der Master die alten Worker ersetzt hat | `Nginx\SystemdReloader` wartet nach dem Reload, bis die alten Worker-PIDs verschwunden sind (max. 5 s) |
| 2026-09-17 | `debugging/smoke-test.sh` brach bei der 444-Prüfung (unbekannter Host) unter `set -e` vorzeitig ab, obwohl der erwartete Status erreicht wurde | `curl` beendet sich bei vom Server geschlossener Verbindung (nginx-Code 444) mit Exit-Status 52; unter `set -e` bricht das die Zeile trotz korrekt geschriebenem `%{http_code}` ab | `expect()` fängt den Exit-Status von `curl` mit `\|\| true` ab und prüft danach nur den geschriebenen HTTP-Code |
| 2026-09-17 | `debugging/smoke-test.sh` meldete bei größeren Antworten gelegentlich einen Fehler, obwohl die gesuchte Zeichenkette vorhanden war | `curl \| grep -q` unter `set -o pipefail`: `grep -q` beendet sich nach dem ersten Treffer und schließt die Pipe, `curl` kann dadurch mit SIGPIPE abbrechen, was `pipefail` als Fehler des gesamten Kommandos wertet | Umgestellt auf `grep -q ... <<<"$(curl ...)"` (Kommandosubstitution statt Pipe an `grep`) |
| 2026-09-17 | `debugging/smoke-test.sh` ließ bei einem Fehlschlag Test-vHosts, Docroots und das Cookie-Tempfile zurück, was den nächsten Lauf blockierte | Aufräumen stand nur am Skriptende, `set -e` beendet das Skript aber schon bei der ersten fehlgeschlagenen Prüfung vorher | `cleanup()` läuft jetzt über `trap cleanup EXIT` bei jedem Skriptende (Erfolg, Fehler, Signal) und zusätzlich einmal vor den eigentlichen Prüfungen, um Reste eines abgebrochenen Vorlaufs zu entfernen |
| 2026-09-17 | Kritisch: Datenbank für `www-data` beschreibbar, root-CLI revalidierte ihren Inhalt beim Rendern nicht (manipulierte Zeile konnte root zu beliebigen nginx-Direktiven/Pfaden verleiten) | Vertrauensgrenze Oberfläche↔CLI existierte nur im Code, nicht in den Dateirechten (`www-data:www-data 0770`/`0660`) | Datenbank auf `root:www-data 0750`/`0640` verengt (`fixDatabasePermissions()`, `install.sh`); `Vhost` revalidiert Name/Port/Unterordner im Konstruktor erneut gegen die Wertobjekte |
| 2026-09-17 | Hoch: `--purge` löschte bei `/var/www/../../etc` trotz Prüfung | `str_starts_with()` auf dem unaufgelösten Pfad ignoriert `..` | Pfad wird mit `realpath()` aufgelöst und verglichen; Exit-Status von `rm -rf` wird ausgewertet |
| 2026-09-17 | Hoch: `own()`/`writeIndex()`/`makeDirectories()` folgten Symlinks in für `www-data` schreibbaren Docroots (Weg zu root über untergeschobenen Symlink) | Keine `is_link()`-Prüfung vor `chown`/`chmod`/Schreiben | Alle vier Stellen lehnen einen Symlink jetzt mit Ausnahme ab, statt ihm zu folgen |
| 2026-09-17 | Mittel/hoch: sudoers erlaubte `www-data` beliebige Argumente, auch `remove --purge`, das die Oberfläche nie braucht | Regel ohne Musterbeschränkung | `Cli\Application` lehnt `--purge` ab, wenn `SUDO_USER=www-data`; sudoers-Datei entsprechend kommentiert |
| 2026-09-17 | Wichtig: `server_name _;` der Oberfläche erlaubte DNS-Rebinding über einen SSH-Tunnel und damit CSRF-Umgehung | Kein Host-Header-Check | `server_name localhost 127.0.0.1;` plus vorgeschalteter `default_server`-Block mit `return 444;` |
| 2026-09-17 | Wichtig: `render()` schrieb Dateien/Symlink vor `nginx -t`; ein Fehlschlag blockierte jeden weiteren Reload inklusive certbot-Hook | Keine Rücknahme bei gescheitertem Reload | Vorzustand wird gemerkt und bei einem Fehler wiederhergestellt |
| 2026-09-17 | Wichtig, **selbst eingeführte Regression der vorigen Zeile**: `purgeBaseDir()` löste den Basisordner per `realpath()` auf und löschte bei einem Symlink dessen Ziel statt des Links; der Gleichheitsfall „Basispfad = Web-Wurzel“ bestand die Prüfung ebenfalls (hätte `/var/www` komplett löschen können) | Die Symlink-Ablehnung der vorigen Zeile deckte `purgeBaseDir()` nicht ab; im Re-Review gefunden | `is_link()`-Prüfung vor jeder Auflösung, Gleichheitsfall ausgeschlossen; fünf neue Tests, drei davon vor dem Fix nachweislich rot |
| 2026-09-17 | Minor: Rücknahme in `render()` löschte eine vorbestehende reguläre Datei an der Stelle des `sites-enabled`-Symlinks | Rücknahme unterschied nicht zwischen „war vorher ein Symlink“ und „lag vorher überhaupt etwas dort“ | `$enabledExistedBefore` statt `$symlinkExistedBefore` steuert die Rücknahme |
| 2026-09-17 | Minor: unbekannter Wert in der Spalte `kind` warf `\ValueError` statt `\RuntimeException` (Oberfläche lieferte HTTP 500 statt kontrollierter Meldung) | `VhostKind::from()` statt `tryFrom()` | `Vhost::fromRow()` nutzt `VhostKind::tryFrom() ?? throw new \RuntimeException(...)` |
